export class Keep {
  constructor(public id:number, public img:string, public description:string, public author:string, public authorId:string, public views:number, public keeps:number, public publicPrivate:boolean) {};
}